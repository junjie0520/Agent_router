# Agent Router 设计文档

> 面向 Agent 场景的智能模型路由系统

---

## 1. 项目背景与用户场景

### 1.1 问题陈述

Agent 任务和普通聊天不同。一个 Agent 可能会读代码、改文件、调用工具、跑测试、总结长文档、分析日志或执行多步工作流。不同任务对模型的要求差异很大：

| 任务类型 | 模型需求 | 隐私需求 |
|---------|---------|---------|
| 简单问答 | 低成本即可 | 无 |
| 代码修改 | 强模型 + 代码能力 | 无 |
| 敏感数据 | 中等 | TEE / 本地 |
| 长上下文 | 强模型 + 大窗口 | 视内容而定 |
| 工具调用 | 中等+ | 视工具而定 |

如果所有任务固定使用同一个模型，要么成本过高，要么复杂任务效果不足。

### 1.2 典型用户场景

```
场景 A：开发者助手
  用户: "帮我修复这段代码的 bug"
  路由: 识别 has_code=True → 过滤 cheap → 选 strong
  结果: 高质量代码修复，成本 ¥0.001/1k tokens

场景 B：财务分析
  用户: "这是内部财务报告，帮我分析 Q2 趋势"
  路由: 识别 privacy=medium → 过滤 cheap/strong → 选 tee
  结果: 安全环境处理，成本 ¥0.002/1k tokens

场景 C：日常问答
  用户: "今天天气怎么样？"
  路由: 识别 complexity=1, no privacy → 选 cheap
  结果: 快速响应，成本 ¥0.0005/1k tokens
```

---

## 2. 系统架构

### 2.1 架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          Agent Router                               │
│                                                                     │
│  ┌──────────────┐    ┌──────────────────┐    ┌──────────────────┐  │
│  │   用户请求    │───▶│  FeatureExtractor │───▶│   LLMAnalyzer    │  │
│  │  (messages)  │    │  (关键词/正则)    │    │  (GLM-4-Flash)   │  │
│  └──────────────┘    └──────────────────┘    └────────┬─────────┘  │
│                                                       │             │
│                                          complexity, privacy,      │
│                                          has_code, reasoning       │
│                                                       │             │
│                                                       ▼             │
│  ┌──────────────┐    ┌──────────────────┐    ┌──────────────────┐  │
│  │ RouteReceipt │◀───│    RuleEngine    │◀───│   隐私 + 能力     │  │
│  │  (审计证据)  │    │   (规则查表)     │    │     过滤          │  │
│  └──────────────┘    └──────────────────┘    └──────────────────┘  │
│         │                                                          │
│         │  selected_model, reason, cost, signature                 │
│         ▼                                                          │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    候选模型池                                 │  │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────┐               │  │
│  │  │  cheap   │    │  strong  │    │   tee    │               │  │
│  │  │ 能力:60  │    │ 能力:85  │    │ 能力:75  │               │  │
│  │  │ ¥0.0005  │    │ ¥0.001   │    │ ¥0.002   │               │  │
│  │  │ 无TEE ❌ │    │ 无TEE ❌ │    │ TEE ✅   │               │  │
│  │  └──────────┘    └──────────┘    └──────────┘               │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 决策流程图

```
                    ┌──────────────┐
                    │  用户请求     │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  LLM 分析    │
                    │  complexity  │
                    │  privacy     │
                    │  has_code    │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │ privacy=     │
                    │ critical?    │──── 是 ──▶ BLOCKED
                    └──────┬───────┘
                           │ 否
                    ┌──────▼───────┐
                    │ privacy=     │
                    │ medium/high? │──── 是 ──▶ 使用 tee
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │ has_code 或  │
                    │ complexity   │──── 是 ──▶ 过滤 cheap
                    │ >=5?         │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  策略路由    │
                    │              │
                    │ cost_first   │──▶ 最便宜
                    │ quality_first│──▶ 能力最强
                    │ fixed_strong │──▶ 固定strong
                    │ privacy_strict│──▶ 安全+最强
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ RouteReceipt │
                    │ + 模型调用   │
                    └──────────────┘
```

---

## 3. 系统边界与非目标

### 3.1 系统边界（做什么）

| 功能 | 说明 |
|------|------|
| ✅ 任务分析 | 识别复杂度、隐私等级、是否涉及代码 |
| ✅ 模型路由 | 根据策略和约束选择最优模型 |
| ✅ 隐私保护 | 敏感数据强制走 TEE 模型 |
| ✅ 审计记录 | 每次路由生成结构化 RouteReceipt |
| ✅ 策略对比 | 支持 cost/quality/privacy 多策略对比 |
| ✅ 降级处理 | LLM 分析失败时回退规则引擎 |

---

## 4. 关键接口与数据结构

### 4.1 API 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/route` | POST | 单条路由决策 |
| `/route/batch` | POST | 批量路由 |
| `/health` | GET | 健康检查 |

### 4.2 RouteReceipt Schema

```json
{
  "request_id": "uuid",
  "timestamp": "ISO8601",
  "selected_model": "mock-strong",
  "decision_reason": "质量优先：选择能力最强",
  "candidate_models": ["mock-cheap", "mock-strong", "mock-tee"],
  "complexity_score": 4,
  "privacy_level": "none",
  "has_code": true,
  "pii_detected": false,
  "pii_types": [],
  "estimated_cost": 0.00014,
  "estimated_latency_ms": 800,
  "policy": "quality_first",
  "llm_analysis": {
    "complexity": 4,
    "privacy_level": "none",
    "has_code": true,
    "reasoning": "编写函数涉及代码生成...",
    "raw_response": "..."
  },
  "fallback_used": false,
  "signature": "..."
}
```

### 4.3 候选模型配置

```json
{
  "id": "mock-strong",
  "cost_per_1k_tokens": 0.001,
  "capability_score": 85,
  "capabilities": ["code"],
  "is_local": false,
  "tee_enabled": false,
  "latency_ms": 800
}
```

---

## 5. 安全 / 隐私 / 信任模型

### 5.1 隐私等级定义

| 等级 | 定义 | 触发条件 | 路由行为 |
|------|------|---------|---------|
| **none** | 无敏感信息 | 普通对话 | 所有模型可用 |
| **medium** | 个人身份信息 | 身份证、手机号、邮箱、财务数据 | 仅 TEE 模型 |
| **high** | 凭证信息 | 密码、密钥、数据库连接串 | 仅 TEE 模型 |
| **critical** | 极端敏感 | AWS凭证、root密码、支付信息 | **BLOCKED** |

### 5.2 信任假设

| 组件 | 信任级别 | 说明 |
|------|---------|------|
| LLMAnalyzer | 部分信任 | 准确率 96%/100%，有容错机制 |
| RuleEngine | 完全信任 | 确定性规则，可审计 |
| TEE 模型 | 硬件级信任 | 数据不离开可信环境 |
| 非 TEE 模型 | 不信任 | 不可用于敏感数据 |

### 5.3 威胁模型

| 威胁 | 缓解措施 |
|------|---------|
| LLM 分析误判 | 规则兜底 + 隐私容忍表 |
| 敏感数据泄露到非 TEE | 隐私硬约束（规则引擎强制执行） |
| API 密钥泄露 | 环境变量注入，不存代码中 |
| 上游模型不可用 | 降级到规则引擎 |

---

## 6. 主要失败模式与处理策略

| 失败模式 | 触发条件 | 处理策略 | 影响 |
|---------|---------|---------|------|
| LLM 调用失败 | API 超时/网络错误 | 回退规则兜底分析 | 准确率下降 8-12% |
| 无可用模型 | 隐私过滤后候选集为空 | 返回 BLOCKED | 请求被拒绝 |
| JSON 解析失败 | LLM 返回格式错误 | 使用正则提取 + 默认值 | 部分字段丢失 |
| critical 隐私 | 检测到极端敏感数据 | 阻断 + 提示需本地模型 | 请求被拒绝 |
| 策略无效 | 未知策略名 | 选择第一个候选模型 | 非最优路由 |

---

## 7. 当前原型的局限性

| 局限 | 说明 | 影响 |
|------|------|------|
| **Mock 模型评分** | 使用预设 baseline_scores，非真实调用 | 评分不反映真实模型表现 |
| **无本地模型** | critical 隐私直接 BLOCKED | 无法处理极端敏感数据 |
| **单次路由** | 不支持多模型级联/投票 | 无 fallback 到备用模型 |
| **静态策略** | 策略固定，不根据历史自适应 | 无法优化长期成本/质量 |
| **单轮分析** | 主要基于最后一条消息 | 多轮对话可能丢失上下文 |
| **LLM 延迟** | 平均 4 秒 | 实时场景有感知延迟 |

---

## 8. 后续研究或工程扩展方向

| 优先级 | 方向 | 说明 |
|--------|------|------|
| 🟡 **高** | 接入真实模型 | 用真实 API 调用替换 mock 评分 |
| 🟡 **高** | 本地模型支持 | 接入 llama.cpp/ollama，解除 critical 阻断 |
| 🟡 **高** | 动态策略 | 基于历史成功率自适应调整权重 |
| 🟢 中 | 多模型级联 | 支持 primary → fallback → last-resort |
| 🟢 中 | 缓存优化 | 相同/相似任务复用分析结果 |
| 🟢 中 | 成本追踪 | 实际 token 用量统计和成本核算 |
| 🔵 低 | Dashboard | 可视化路由决策和统计面板 |
| 🔵 低 | A/B 测试框架 | 比较不同策略长期效果 |

---

## 附录 A：评测结果摘要

| 策略 | 成本/任务 | 成功率 | 质量分 |
|------|-----------|--------|--------|
| cost_first | ¥0.000154 | 96.0% | 0.82 |
| fixed_strong | ¥0.000154 | 96.0% | 0.82 |
| privacy_strict | ¥0.000253 | 100.0% | 0.76 |
| quality_first | ¥0.000169 | 100.0% | 0.84 |

LLM 分析准确率：**has_code 96.0%** | **privacy 100.0%**

---

## 附录 B：技术栈

| 组件 | 技术 |
|------|------|
| Web 框架 | FastAPI + Uvicorn |
| LLM 分析 | GLM-4-Flash (智谱AI) |
| 规则引擎 | 纯 Python 实现 |
| 测试框架 | pytest + 自定义 benchmark |
| 数据格式 | JSON Schema |
| API 文档 | Swagger (自动生成) |
```